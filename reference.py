# import network
import time
from paho.mqtt import client as MQTTClient
from config import (
    WIFI_SSID, WIFI_PASS,
    MQTT_BROKER, MQTT_USER, MQTT_PASS
)
import json

# wlan = network.WLAN(network.STA_IF)
# wlan.active(True)
# wlan.connect(WIFI_SSID, WIFI_PASS)
# while not wlan.isconnected():
#     print(".", end="")
#     time.sleep(0.5)
# print("local Wifi status: connected")
#
# mqtt = MQTTClient(client_id="",
#                   server=MQTT_BROKER,
#                   user=MQTT_USER,
#                   password=MQTT_PASS)
# mqtt.connect()
# print("broker status: connected")


# try:
#     Date(time)
# except(ValueError):
#     something.config = error
# if isinstance(int, cat)
#     if cat > 0:
#         return
# error_trigger

